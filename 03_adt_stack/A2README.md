## REFLECTION QUESTIONS
### Qn1. What value does topIndex hold immediately after construction, and why does that specific value make isEmpty() return the right answer with no special-casing?
= The constructor sets the topIndex as 0 right after stack is created. TopIndex represents the next free slot in the array. Since nothing has been pushed yet topIndex starts at 0 as there are 0 elements in the array and since isEmpty() only checks whether topIndex == 0 there is no need for a special casing.

### Qn2 Walk through push step by step, in words: what happens to data and topIndex, in what order, and why would swapping that order break the stack?
= So at first the push method through the use of if statement sees if the Stack is full or not. So ifFull() is true then it prints out a statement saying that the Stack is full and ends the method with the return. If it is false then it skips the if statement and at data[topIndex ] which is at position 0 (for topIndex = 0) will be assigned the value. After that the topIndex is incremented by 1.

### Qn3 Give the time complexity of push, pop, peek, isEmpty, isFull, and size, individually, in Big-O. For each one, justify it by describing what your code actually does, not by naming the general rule for stacks.
= push() is O(1) as it does at most one comparison (isFull()) one array assignment (data[topIndex] = value) , and one increment (topIndex++) none of these depend on how many elements are currently in the stack. pop() is O(1) one comparison(isEmpty()) ,  one array read (data[topIndex - 1 ] ), one decrement (topIndex--) . peek() is O(1) one comparison (isEmpty()),  one array read (data[topindex - 1]). isEmpty() is O(1) as it is a single comparison topIndex == 0 . isFull() is O(1) as it is a single comparison topIndex == 100 . size() is O(1) since it directly return topIndex. 

### Qn4 Exactly what happens in your code when push is called while the stack is full? State what prints (if anything), what gets returned (if anything), and whether topIndex changes. Then say why you chose that behavior.
= When push is called while the Stack is empty it will print out a message saying that "The stack is full". Since push() is declared as a void I just used return statement which will exit the function early. I chose this behavior because without the guard push() would use topIndex ( which would be 100) to index into the data[100] but data is initialized only till 100 (0 - 99) so it would be out of bounds. 

### Qn5 Exactly what happens when pop or peek is called while the stack is empty? Same level of detail as above.
= When pop or peek is called when the Stack is empty they would both print out "Stack is Empty". Unlike push() which returns void pop() and peek() both should return int so I used -1 as -1 wouldn't occur from a correctly executed pop() and peek(). And if I didn't use a guard there since the Stack is empty topIndex would be 0 so data[topIndex - 1 ] would become data[-1] which is an index before the start of the array and would give an out of bound error.

### Qn6 Our array is fixed at 100 slots. If a caller needed more room than that, what would have to change in your class to support it, and what would it likely cost in terms of the time complexity you gave in question 3?
= If we want to make more slots then we would need the data to be dynamic rather than fixed and we can do that by using new. And when push() is called and the class is already full it needs to allocate a new and larger array (i.e double the size) and use the loop to transfer old data to the new one. The loop of copying would go through n elemets currently in the old array so the time complexity would be O(n) rather than O(1). 

### Qn7 Without looking anything up: if this stack were built on a linked list instead of an array, name one advantage and one disadvantage that trade-off would bring, based on what you already know about how arrays and pointers behave differently.
= Linked-list doesn't have a fixed slot like stack so you could keep on adding into it since each element is a node and acts as a pointer to the next one. But for stack we can use stuff like data[topIndex - 1] to take a peek at the top element but you cannot do that on linked list as it doesn't have a proper indexing you cannot directly search for an element directly like data[40].